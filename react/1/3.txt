import React from "react";
import { AuditTrailItem } from "../interfaces/auditTrail.interface";
import { auditTrailData } from "../mockData/auditTrail";
import "./AuditTrail.scss";

const AuditTrail: React.FC = () => {
  return (
    <div className="audit-trail">
      <div className="audit-header">
        <span className="icon">⟲</span>
        <h3>Audit Trail</h3>
        <span className="count">{auditTrailData.length}</span>
      </div>

      <div className="timeline">
        {auditTrailData.map((item: AuditTrailItem) => (
          <div className="timeline-item" key={item.id}>
            <div className="dot" />

            <div className="content">
              <h4>{item.title}</h4>
              <p className="description">{item.description}</p>

              <div className="meta">
                <span className="user">👤 {item.user}</span>
                <span className="time">
                  🕒 {item.date} {item.time}
                </span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default AuditTrail;
