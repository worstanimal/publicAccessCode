export type LevelType = 'STANDARD' | 'AREA' | 'TOPIC' | 'METRIC';

export interface HierarchyNode {
  id: string;
  name: string;
  level: LevelType;
  weightage: number;
  override?: boolean; // true = editable (METRIC only)
  children?: HierarchyNode[];
}
